# we-aigo 核心闭环测试指南

## 已完成部署

### 1. Cloudflare Worker (DeepSeek API 代理)
- **URL**: https://we-aigo-worker.we-aigo-api.workers.dev
- **功能**: 接收任务执行请求，调用 DeepSeek API，返回结果并写入数据库
- **路由**:
  - `POST /execute` - 执行任务
  - `GET /health` - 健康检查

### 2. Cloudflare Pages 前端
- **URL**: https://300c2867.we-aigo.pages.dev
- **功能**: 完整的任务发布、工作台、创作工坊界面

---

## 测试流程

### 方式一：通过浏览器测试

1. **访问前端**
   - 打开 https://300c2867.we-aigo.pages.dev
   - 登录后进入"发布任务"页面

2. **发布任务**
   - 选择任务类型（如"内容创作"）
   - 填写任务标题：`AI诗歌创作`
   - 填写任务描述：`请用中文写一首关于春天的七言绝句`
   - 点击"发布并自动匹配 AI"

3. **进入工作台**
   - 任务发布后自动跳转到工作台页面 `/workspace/{taskId}`
   - 点击"启动 AI 执行"按钮

4. **等待执行**
   - 页面显示真实执行进度日志
   - 等待 30-60 秒（DeepSeek API 响应时间）
   - 执行完成后自动刷新，显示交付物

5. **验收交付物**
   - 查看 AI 生成的内容
   - 可以复制、展开/收起
   - 点击"前往验收"完成闭环

### 方式二：通过 API 测试

```bash
# 1. 创建任务
curl -X POST "https://mzjmfyoemcsoqzoooiej.supabase.co/rest/v1/tasks" \
  -H "Content-Type: application/json" \
  -H "apikey: YOUR_ANON_KEY" \
  -H "Authorization: Bearer YOUR_ANON_KEY" \
  -d '{
    "title": "测试任务",
    "description": "请用中文回复：你好",
    "publisher_id": 1,
    "budget": 10,
    "status": "matched",
    "matched_agent_id": 1
  }'

# 2. 获取任务 ID，然后执行
curl -X POST "https://we-aigo-worker.we-aigo-api.workers.dev/execute" \
  -H "Content-Type: application/json" \
  -d '{
    "taskId": 你的任务ID,
    "title": "测试任务",
    "description": "请用中文回复：你好",
    "taskType": "搜索整理"
  }'

# 3. 查询交付物
curl "https://mzjmfyoemcsoqzoooiej.supabase.co/rest/v1/deliveries?task_id=eq.任务ID" \
  -H "apikey: YOUR_ANON_KEY"
```

---

## 核心代码文件

### Worker 端
- `/app/data/所有对话/主对话/we-aigo/worker/src/index.ts` - Worker 主代码
- `/app/data/所有对话/主对话/we-aigo/worker/wrangler.toml` - 配置文件

### 前端
- `/app/data/所有对话/主对话/we-aigo/frontend/src/pages/AgentWorkspacePage.tsx` - 工作台页面
- `/app/data/所有对话/主对话/we-aigo/frontend/src/pages/CreateTaskPage.tsx` - 任务发布页面
- `/app/data/所有对话/主对话/we-aigo/frontend/src/pages/CreateWorkshop.tsx` - 创作工坊

---

## 架构说明

```
用户操作流程:
┌──────────────────────────────────────────────────────────────┐
│  发布任务 (CreateTaskPage)                                    │
│  - 选择任务类型                                                │
│  - 填写标题描述                                                │
│  - 状态设为 matched，自动匹配 Agent 1                          │
└─────────────────────┬────────────────────────────────────────┘
                      ▼
┌──────────────────────────────────────────────────────────────┐
│  工作台 (AgentWorkspacePage)                                  │
│  - 显示"启动 AI 执行"按钮                                     │
│  - 用户点击后调用 Worker API                                  │
└─────────────────────┬────────────────────────────────────────┘
                      ▼
┌──────────────────────────────────────────────────────────────┐
│  Cloudflare Worker (/execute)                                │
│  - 接收 taskId, title, description                           │
│  - 自动推断任务类型                                            │
│  - 调用 DeepSeek API (deepseek-chat)                         │
│  - 写入 deliveries 表                                         │
│  - 更新 tasks 状态为 completed                                │
└─────────────────────┬────────────────────────────────────────┘
                      ▼
┌──────────────────────────────────────────────────────────────┐
│  工作台刷新显示                                                │
│  - 自动刷新获取交付物                                         │
│  - 用户验收完成闭环                                           │
└──────────────────────────────────────────────────────────────┘
```

---

## 支持的任务类型

| 类型 | 系统提示词 |
|------|-----------|
| 搜索整理 | 信息搜索和整理助手 |
| 内容创作 | 创作高质量原创内容 |
| 数据分析 | 数据分析和建议 |
| 翻译润色 | 翻译和文字优化 |
| 学习辅导 | 知识讲解和答疑 |

---

## 注意事项

1. **网络延迟**: DeepSeek API 响应可能需要 10-60 秒，请耐心等待
2. **超时设置**: Worker 超时设置为 60 秒
3. **任务推断**: 如果不传 taskType，Worker 会自动从标题和描述推断
4. **安全**: 所有 API Key 都在 Worker secrets 中，不暴露在前端
