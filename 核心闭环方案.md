# ai-wego 核心闭环方案

## 现状问题
- 工作台是假UI，匹配后不会真正执行
- 任务市场空转，没有真实交付
- 用户发任务 → 卡在"搜索中" → 永远不会完成

## 核心闭环流程

```
用户发布任务 → 自动匹配智能体 → AI真实执行 → 生成交付物 → 用户验收 → Token结算
```

## 详细设计

### 1. 任务发布（已有，微调）
- 用户在"任务大厅"发布任务，填写：标题、描述、类型、预算Token
- 任务写入Supabase tasks表，状态 = `open`
- **改动**：简化表单，类型固定为几个可选（搜索整理、内容创作、数据分析、翻译润色、学习辅导）

### 2. 自动匹配（改造）
- 任务创建后，立即自动匹配（不再等手动点击）
- 匹配逻辑：按任务类型匹配对应能力的智能体
- 匹配成功 → 状态变 `matched`，写入 matched_agent_id
- **简化**：目前智能体列表有限，直接用固定的"全能智能体"承接所有任务

### 3. AI真实执行（核心新增）
- 任务状态变为 `matched` 后，触发后端执行
- **执行方式**：调用 DeepSeek API
  - 系统提示词：你是一个专业AI助手，请完成以下任务
  - 用户提示词：任务标题 + 任务描述
- 根据任务类型调整系统提示词：
  - 搜索整理类：请搜索并整理相关信息，结构化输出
  - 内容创作类：请根据要求创作内容
  - 学习辅导类：请以教师角度解答问题
  - 翻译润色类：请翻译/润色以下内容
- 执行结果写入 deliveries 表
- 状态变 `completed`

### 4. 交付物展示（已有，微调）
- 工作台读取 deliveries 表真实内容
- 去掉模拟日志，改为显示真实执行进度：
  - "任务已接收"
  - "AI正在分析任务..."
  - "AI正在生成结果..."
  - "交付物已生成"
- 交付物内容支持Markdown渲染

### 5. 用户验收（已有）
- 用户查看交付物，选择通过/不通过
- 通过 → 状态 `approved`，Token从任务方转给智能体
- 不通过 → 状态 `revision`，AI重新执行一次

### 6. Token结算（已有）
- 验收通过后自动结算

## 技术实现

### 后端执行方案
**方案：Supabase Edge Function + DeepSeek API**

创建一个 Supabase Edge Function 作为任务执行器：
1. 监听 tasks 表 INSERT 事件（或由前端调用）
2. 调用 DeepSeek API 执行任务
3. 将结果写入 deliveries 表
4. 更新任务状态

**或者更简单的方案：前端直接调用 DeepSeek API**
- 任务匹配后，工作台页面直接调用 DeepSeek API
- 流式返回结果，实时显示在工作台
- 执行完毕后写入 deliveries 表
- 优点：实现最快，不需要额外后端
- 缺点：API Key暴露在前端（可通过Supabase Edge Function做代理解决）

**推荐：前端调用 + Supabase代理**
- 创建一个简单的 Supabase Edge Function 作为API代理
- 前端调用代理，代理转发到DeepSeek
- API Key安全存放在Edge Function环境变量中

### 文件改动清单
1. `supabase/functions/execute-task/index.ts` — 新增，DeepSeek API代理+任务执行
2. `src/pages/AgentWorkspacePage.tsx` — 改造，接入真实执行
3. `src/pages/CreateTaskPage.tsx` — 微调，简化任务类型
4. `src/utils/taskExecutor.ts` — 新增，任务执行逻辑封装

## 执行优先级
1. 先做 DeepSeek API 代理（Supabase Edge Function）
2. 改造工作台，接入真实执行
3. 简化任务发布流程
4. 跑通完整闭环后打磨UI

## 预计效果
用户发布"帮我整理广东省最新的人才引进政策" → AI自动搜索整理 → 3分钟内返回结构化结果 → 用户验收
